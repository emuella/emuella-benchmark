# Retained MQ D2 confirmation result

MQ D2 was **not selected** under the prospective incremental policy. The primary
PAN16 estimate saved 5.47 ms (0.32%), below the predeclared 20 ms and 2% floors;
its interval also crossed zero. MSI16 met its required +1% upper-bound
non-regression check. Production arithmetic is to remain the baseline form.
This result does not establish no effect or a general decoder ranking.

The [new protocol](mq-d2-incremental-confirmation.md) selected
`incremental-performance-policy/v1`, Route A, at benchmark
`8a5202d98a9c4f08b8b0d2c09823d44a9b35ae72`. The
[factual evidence](evidence/mq-d2-incremental-confirmation.json) retains all forty
per-arm operation times, source/build/input/stream identities, per-case process
RSS/CPU, complete resource observations and original failed count gate. The
previous entropy experiment's protocol and rejection remain unchanged.

## Complete fixed primary block

Both products are full previously measured Boca Raton acquisitions, style zero,
one worker, common immutable OpenJPEG-origin streams. Each contrast contains
20 adjacent alternating AB/BA fresh-process pairs, zero warmups and one public
facade operation per process. Negative change favours D2.

| Product and role | Baseline mean ms | D2 mean ms | Saving ms | Mean change | Conservative 99% interval | Legacy timing verdict |
|---|---:|---:|---:|---:|---|---|
| PAN16, sole promotion primary | 1714.339 | 1708.867 | 5.472 | −0.319% | [−0.950%, +0.315%] | equivalent within ±5% |
| MSI16, mandatory corroboration | 937.692 | 936.122 | 1.569 | −0.167% | [−1.047%, +0.720%] | equivalent within ±5% |

PAN16 needed estimated reduction >=2%, upper interval bound <0 and absolute
saving >=20 ms. All three predicates fail. MSI16's upper bound is below +1%.
The disposition is `not-selected`: insufficient estimated magnitude and absolute
impact already fail the primary, so this is not solely an insufficient-precision
outcome. The unchanged estimator's equivalent verdict does not override these
prospective engineering criteria. These intervals are per-case, not family-wide.

All 80 ordinary calls completed with exact full native samples and the required
stream hashes. Each arm independently met the fixed operation/process/admission
limits. Baseline/candidate maximum observed process RSS was respectively
247,386,112 and 247,345,152 bytes; this is whole-process RSS, not facade allocation.
The completed primary failure stopped all 14 regression contrasts and all
80 conditional resource calls before launch. No extra rounds, replacements,
outlier removal, pooling, screening, retuning or external timings occurred.

## Correctness, resources and the pre-timing correction

Independent source review classified the restored D2 mechanism as a compact
local arithmetic refactor with no new allocation path, buffer, persistent state,
backend, scheduler, option, unsafe/ISA dependency or material failure surface.
The candidate passed its current independent MQ/raw oracle and canonical checks,
including scratch reuse after rejection. Its full source binding is
`07c85ecac1fbe1c477c05a8f6c8d605d1039d9ad`; baseline is
`e0971bc82d18d787a47c53ff27ed51a81e70b94c`.

Before timing, 64 separate allocation-build calls covered both arms, both Boca
products, both styles, both unchanged stream origins and 1/2/4/8 workers. Another
16 calls proved complete unchanged Mansfield PAN16 encoded bytes, both styles
and 1/2/4/8 workers in both arms. Every call checked every native sample and
passed its absolute bounds. The maximum requested additional live allocation
was 233,441,260 bytes; all peaks fit both 768 MiB and the existing conservative
geometry/encode working query. This query is a comparison bound, not a decoder
requirements API. Independent OpenJPEG receipts were reused only after matching
exact stream and raw hashes and their original build/response provenance.

The original prose required allocation-count equality. Its evaluator correctly
reported failure in 16 four/eight-worker decode pairs; the original verdict is
retained. All 32 paired decoder peaks were exactly equal, and counts matched
for one/two-worker decode and every encode pair. Mixed-direction count drift was
independently reviewed **after resource observations and before any timing**.
The unchanged decoder creates scratch with per-job `map_init`/`try_for_each_init`;
the pinned Rayon work splitting depends on stealing, so successful allocation
request counts are not a deterministic resource contract. The review established
a concrete variability mechanism, not event-level numerical attribution.

[Resource correction 1](https://github.com/emuella/emuella-workspace/blob/d27e9bc3bf2b7cf8b661b22143c55c61f327039d/docs/plans/active/mq-d2-incremental-confirmation.md)
restored the original conditional intent: equality where deterministic, otherwise
retain and independently review count drift, require unchanged allocation paths
and every absolute bound. It used one setup repair with no reruns or added calls;
it did not alter any timing predicate, numerical resource cap, estimator,
candidate, build, input or twenty-pair budget. The immutable original freeze,
failed count verdict and separately recorded correction remain available. No
unchanged allocation-frequency claim is made.

## Bounds and retention

Both arms used the same compiled benchmark worker source, Rust 1.97.1, perf
optimisation 3, ThinLTO, one codegen unit, parallel enabled and SIMD disabled.
The encoder selector stayed unset and original W scheduling remained selected.
Headline calls used direct/global facade execution on CPU 0; resource probes
used the requested first 1/2/4/8 CPUs. The host was an AMD Ryzen 9 9950X3D with
the recorded governor/topology. Allocation diagnostics emitted no headline samples.

Exactly 160 of the maximum 800 experimental codec calls ran: 80 prerequisites and 80 primary
observations. All 640 conditional calls remained unstarted. Separate delivery
integration checks are not efficacy observations and do not reopen these stages. The source-only
candidate remains recoverable with its fresh not-selected outcome; rejection
is not permission to discard its mechanism or overwrite historical evidence.
The codec owner records restoration and inventory disposition. Protected inputs,
streams, raw observations and complete build receipts remain in the original
approved persistent store with notices and lineage; only factual evidence is
committed here.

RarePlanes Dataset, June 2020: J. Shermeyer et al.; In-Q-Tel – CosmiQ Works and
AI.Reverie, CC BY-SA 4.0. The conditional SpaceNet stage was not started.
